# AutoOps Orchestrator — Dependencies
# Requires Python >= 3.10

# LangGraph — StateGraph, conditional edges, fan-out/fan-in
langgraph>=0.4.0

# LangChain core — base types used by LangGraph
langchain-core>=0.3.0

# FastAPI — web framework
fastapi>=0.115.0

# Uvicorn — ASGI server
uvicorn[standard]>=0.34.0

# MCP — official Model Context Protocol SDK (raw SDK with FastMCP)
mcp>=1.0.0

# python-dotenv — .env file loading for DEMO_MODE
python-dotenv>=1.0.0

# Pydantic — strict model validation
pydantic>=2.7.0

# LLM / embedding providers used by Member 2 nodes
openai>=1.40.0
anthropic>=0.34.0
groq>=0.9.0
psycopg[binary]>=3.2.0

# Resend — HITL escalation emails (Member 1)
resend>=2.1.0

# httpx — async HTTP client for Supabase Realtime broadcast
httpx>=0.27.0

# Supabase — database client
supabase